function projectCreation(name, projectsNumber){
    let timeNeeded = Number(projectsNumber) * 3;
    console.log(`The architect ${name} will need ${timeNeeded} hours to complete ${projectsNumber} project/s.`);
}
projectCreation("Sanya", "9");
